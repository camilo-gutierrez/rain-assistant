services:
  rain-assistant:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    volumes:
      - ~/.rain-assistant:/root/.rain-assistant
    restart: unless-stopped
